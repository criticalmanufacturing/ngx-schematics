{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "SchematicsPackageInfo",
  "title": "Package Info Options Schema",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["project"]
}
